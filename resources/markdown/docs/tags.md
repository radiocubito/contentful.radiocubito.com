# Tags

## The Tag Object

The tag object that is accessed via Eloquent queries provides a variety of useful methods for inspecting the post's attributes and relationships:

```php
// Get all of the tag's posts...
$tag->posts : Illuminate\Database\Eloquent\Collection
```
